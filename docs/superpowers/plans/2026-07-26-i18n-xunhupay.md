# i18n + Triple Checkout Implementation Plan

> **For agentic workers:** Implement task-by-task. Steps use checkbox syntax.

**Goal:** Chinese/English UI toggle + pricing with Paddle, WeChat, and Alipay (虎皮椒).

**Architecture:** LocaleProvider + message dictionaries; payment adapter for Xunhuupay alongside existing Paddle; `cn_orders` in SQLite; same Pro subscription row by email.

**Tech Stack:** Next.js App Router, React client i18n, node:sqlite, Xunhuupay HTTP API, existing Paddle.js

---

### Task 1: i18n foundation
- Create `src/lib/i18n/messages.ts`, `src/components/LocaleProvider.tsx`, `LocaleToggle.tsx`
- Wire layout/header

### Task 2: Translate main UI
- Home, AuthButton, SiteFooter, pricing, verify, legal pages use `t()`

### Task 3: CN orders + Xunhuupay lib
- Schema in `db.ts`, `src/lib/xunhupay.ts`, order helpers

### Task 4: Pay APIs
- `create`, `notify`, `status` routes

### Task 5: Pricing UI three methods
- Paddle + WeChat/Alipay QR modal + email gate

### Task 6: Deploy + smoke
- rsync, build, restart; verify locale toggle; CN buttons degrade if no env
